// MARK: - Timetable View
// Owner: UI
//
// Displays the weekly timetable as a grid/calendar view.
// Shows:
// - Fixed classes (from UCL timetable) in one colour
// - Scheduled study sessions (from algorithm) in another colour
// - Free time slots
//
// Binds to TimetableViewModel for data.

import SwiftUI

struct TimetableView: View {
    var body: some View {
        // TODO: Implement weekly timetable grid
        Text("Timetable View")
    }
}
