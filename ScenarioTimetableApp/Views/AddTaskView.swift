// MARK: - Add Task View
// Owner: Josh
//
// A form for creating a new study task.
// Fields:
// - Task title (text)
// - Subject (text or picker)
// - Deadline (date picker)
// - Priority (segmented control: High / Medium / Low)
// - Estimated hours (stepper or number field)
//
// Submits to TaskViewModel.addTask()

import SwiftUI

struct AddTaskView: View {
    var body: some View {
        // TODO: Implement task creation form
        Text("Add Task View")
    }
}
