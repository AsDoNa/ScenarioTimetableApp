// MARK: - Task Row View (Component)
// Owner: UI
//
// A reusable component that renders a single task row
// in the task list. Used by TaskListView.
// Shows: title, subject, deadline, priority indicator, completion status.

import SwiftUI

struct TaskRowView: View {
    var body: some View {
        // TODO: Implement task row
        Text("Task Row")
    }
}
