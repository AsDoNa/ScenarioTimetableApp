// MARK: - Time Slot View (Component)
// Owner: Josh
//
// A reusable component that renders a single time block
// in the timetable grid. Used by TimetableView.
// Should visually distinguish between:
// - Timetable classes (e.g., blue)
// - Study sessions (e.g., green)
// - Free time (e.g., grey/empty)

import SwiftUI

struct TimeSlotView: View {
    var body: some View {
        // TODO: Implement time slot cell
        Text("Time Slot")
    }
}
