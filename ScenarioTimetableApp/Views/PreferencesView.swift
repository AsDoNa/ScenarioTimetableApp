// MARK: - Preferences View
// Owner: UI
//
// Allows the user to set their scheduling preferences:
// - Preferred study hours (start/end time pickers)
// - Max session length (slider or stepper)
// - Break duration between sessions
// - Days off (multi-select)
// - Weekly study goal (hours)
//
// Saves via TaskViewModel or a dedicated PreferencesViewModel.

import SwiftUI

struct PreferencesView: View {
    var body: some View {
        // TODO: Implement preferences form
        Text("Preferences View")
    }
}
